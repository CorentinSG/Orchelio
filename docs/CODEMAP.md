# Orchelio — Code map

> Generated by `npm run codemap`. Do not edit by hand.

Every source module, what it is for, and what it exports. Read this before
searching the codebase: it is one file instead of eighty, and it starts from the
whole picture rather than a keyword match.

For questions about how modules *reach* each other — call paths, hubs, unexpected
coupling — use the knowledge graph instead: `npm run graph:explain -- "someSymbol"`.
See `docs/HARNESS.md`.

Modules: 81.

## `prisma/`

### `prisma/schema.prisma`

Models: `User`, `Session`, `Firm`, `FirmMembership`, `FirmConfiguration`, `PracticeArea`, `MatterType`, `WorkflowTemplate`, `ClientProfile`, `Matter`, `IntakeResponse`, `Document`, `FirmWorkflow`, `WorkflowRun`, `WorkflowStep`, `AIAnalysis`, `AIReview`, `ApprovalRequest`, `Task`, `DraftCommunication`, `UsageRecord`, `AuditEvent`

### `prisma/seed.ts`

Everything created here is fictional.

## `scripts/`

### `scripts/codemap.mjs`

Writes docs/CODEMAP.md: every source module, its exported symbols, and the first line of its file-level comment.

### `scripts/doctor.mjs`

Answers one question: is this checkout ready to work in? It checks the things that actually go wrong — a missing `.env`, an un-migrated database, an empty seed, a stal…

## `src/`

### `src/middleware.ts`

READ THIS BEFORE CHANGING IT: this middleware is a convenience, not a security boundary.

Exports: `middleware`, `config`

## `src/app/`

### `src/app/(app)/admin/firms/page.tsx`

Platform administration — firms.

Exports: `metadata`, `dynamic`, `AdminFirmsPage`

### `src/app/(app)/dashboard/page.tsx`

Widgets whose data arrives later show a dash rather than a misleading zero.

Exports: `metadata`, `dynamic`, `DashboardPage`

### `src/app/(app)/layout.tsx`

Layout for every signed-in page.

Exports: `dynamic`, `AppLayout`

### `src/app/(app)/onboarding/[step]/page.tsx`

Exports: `metadata`, `dynamic`, `OnboardingStepPage`

### `src/app/(app)/onboarding/page.tsx`

`/onboarding` resumes wherever the firm left off, so a half-finished questionnaire is picked up rather than restarted.

Exports: `dynamic`, `OnboardingPage`

### `src/app/403/page.tsx`

403.

Exports: `metadata`, `ForbiddenPage`

### `src/app/api/firms/switch/route.ts`

A plain form POST answered with a 303 — see src/lib/http/form-post.ts for why Orchelio does not use a Server Action here.

Exports: `POST`

### `src/app/api/onboarding/route.ts`

A plain form POST answered with a 303.

Exports: `POST`

### `src/app/error.tsx`

Application error screen.

Exports: `GlobalError`

### `src/app/layout.tsx`

Exports: `metadata`, `viewport`, `RootLayout`

### `src/app/loading.tsx`

Loading screen.

Exports: `Loading`

### `src/app/login/actions.ts`

These are Server Actions, which Next.js protects against cross-site requests by checking the request origin.

Exports: `signInAction`, `signOutAction`, `SignInState`

### `src/app/login/login-form.tsx`

Sign-in form.

Exports: `LoginForm`

### `src/app/login/page.tsx`

Sign-in is always rendered fresh: it depends on the caller's session cookie.

Exports: `metadata`, `dynamic`, `LoginPage`

### `src/app/not-found.tsx`

404.

Exports: `metadata`, `NotFound`

### `src/app/page.tsx`

Orchelio home page.

Exports: `dynamic`, `HomePage`

## `src/components/`

### `src/components/app-shell.tsx`

The sidebar always names the product and, underneath, the firm currently open.

Exports: `AppShell`

### `src/components/brand.tsx`

Orchelio wordmark.

Exports: `OrchelioMark`, `OrchelioWordmark`, `OrchelioTagline`

### `src/components/demo-banner.tsx`

Standing demonstration warning.

Exports: `DemoBanner`

### `src/components/firm-switcher.tsx`

Firm switcher.

Exports: `FirmSwitcher`

### `src/components/onboarding-ui.tsx`

The audience is a lawyer, not an administrator: every control is a labelled checkbox or a plain field, every option says what it means, and nothing is hidden behind an…

Exports: `ProgressBar`, `Field`, `CheckboxOption`, `LockIcon`, `StepActions`, `WorkflowPreview`, `inputClass`

### `src/components/ui.tsx`

Deliberately small: cards, badges and callouts, built directly on the design tokens in globals.css.

Exports: `Badge`, `StatusDot`, `Card`, `Callout`, `DataRow`, `CommandLine`, `Tone`

## `src/lib/`

### `src/lib/app-config.ts`

Product naming lives in exactly one place so that the sidebar, the login page, the page titles, the metadata, the loading screens and the error screens can never drift…

Exports: `APP_NAME`, `APP_ENV`, `IS_DEMO`, `APP_EDITION`, `APP_FULL_NAME`, `APP_TAGLINE`, `APP_DESCRIPTION`, `POWERED_BY`, `DEMO_NOTICE_SHORT`, `DEMO_NOTICE_LONG`, `FICTIONAL_DATA_NOTICE`

### `src/lib/audit.ts`

Append-only: this module exposes exactly one write function, and no update or delete path for `AuditEvent` exists anywhere in the codebase.

Exports: `recordAuditEvent`, `newCorrelationId`, `recordViewEvent`, `AuditInput`

### `src/lib/auth/firm-context.ts`

A user may belong to more than one firm.

Exports: `setActiveFirmCookie`, `clearActiveFirmCookie`, `resolveActiveFirm`, `activeFirmFor`, `scopeFor`

### `src/lib/auth/guards.ts`

These guards run on the server, inside the page or action that needs them.

Exports: `requireSession`, `requireFirmAccess`, `requirePermission`, `requirePlatformAdmin`, `requireFirmScope`, `FirmContext`

### `src/lib/auth/password.ts`

scrypt from the Node.js standard library: memory-hard, no dependency, and available everywhere the application runs.

Exports: `hashPassword`, `verifyPassword`, `DUMMY_PASSWORD_HASH`

### `src/lib/auth/permissions.ts`

Roles are coarse; permissions are fine.

Exports: `isFirmRole`, `can`, `permissionsFor`, `roleLabel`, `FIRM_ROLES`, `ROLE_LABELS`, `PLATFORM_ADMIN_LABEL`, `PERMISSIONS`, `FirmRole`, `Permission`, `Actor`

### `src/lib/auth/rate-limit.ts`

A deliberately simple in-memory fixed-window counter, keyed by the submitted email address.

Exports: `consumeAttempt`, `resetAttempts`, `resetAllAttempts`, `RateLimitResult`

### `src/lib/auth/session.ts`

Sessions are server-side and revocable.

Exports: `createSession`, `destroySession`, `actorFor`, `currentSession`, `SessionFirm`, `SessionUser`, `Session`

### `src/lib/cache.ts`

## The rule **Firm-scoped data is never cached across requests.

Exports: `platformCatalogue`, `clearCatalogueCache`, `catalogueCacheSize`, `requestScoped`, `UncacheableModelError`, `CACHEABLE_MODEL_NAMES`

### `src/lib/constants.ts`

SQLite has no native enums, so the database stores plain strings and this module is the single source of truth for what those strings may be.

Exports: `isLockedApproval`, `USER_STATUSES`, `FIRM_STATUSES`, `MEMBERSHIP_STATUSES`, `GENERIC_MATTER_STATUSES`, `EMPLOYMENT_MATTER_STATUSES`, `MATTER_STATUSES`, `REPRESENTATION_SIDES`, `AI_STATUSES`, `ALLOWED_DOCUMENT_EXTENSIONS`, `ALLOWED_DOCUMENT_MIME_TYPES`, `MAX_DOCUMENT_SIZE_BYTES`, `DOCUMENT_ANALYSIS_STATUSES`, `APPROVAL_DECISIONS`, `DECISIONS_REQUIRING_NOTE`, `RISK_LEVELS`, `CONFIGURABLE_APPROVALS`, `LOCKED_APPROVALS`, `AUDIT_STATUSES`, `AUDIT_ACTIONS`, `UserStatus`, `FirmStatus`, `MembershipStatus`, `MatterStatus`, `RepresentationSide`, `AiStatus`, `ApprovalDecision`, `RiskLevel`, `LockedApproval`, `AuditStatus`, `AuditAction`

### `src/lib/dashboard/widgets.ts`

The dashboard is assembled from the firm's configuration, not chosen by the code.

Exports: `widgetsFor`, `widgetValue`, `WidgetTone`, `DashboardWidget`

### `src/lib/data/activity.ts`

Writing is in `src/lib/audit.ts`; this module only reads, and only ever for one firm.

Exports: `listActivity`, `countActivity`, `activityActions`, `ActivityFilters`

### `src/lib/data/analyses.ts`

Analyses hold extracted facts about a client's situation, so they are among the most sensitive records in the product.

Exports: `getAnalysis`, `listAnalysesForMatter`, `listRecentAnalyses`, `getReview`, `countAnalyses`

### `src/lib/data/catalogues.ts`

Practice areas, matter types and workflow templates are identical for every firm on the instance.

Exports: `matterTypesForPracticeAreas`, `workflowTemplatesFor`, `allPracticeAreas`, `allMatterTypes`, `allWorkflowTemplates`

### `src/lib/data/documents.ts`

A document is reachable only through its own firm.

Exports: `getDocument`, `listDocuments`, `countDocuments`, `getDocumentForDownload`

### `src/lib/data/firm-scope.ts`

The single rule this product depends on: a query for a firm-scoped resource must say which firm it is for.

Exports: `assertFirmScoped`, `withFirmScopeGuard`, `FIRM_SCOPED_MODELS`, `PLATFORM_MODELS`, `FirmScopeError`, `GuardedPrismaClient`

### `src/lib/data/firms.ts`

`Firm` itself is not firm-scoped — it *is* the firm — but everything hanging off it is, so those reads go through a scope like every other.

Exports: `firmConfiguration`, `firmMembers`, `enabledWorkflows`, `getFirm`

### `src/lib/data/matters.ts`

Note the shape of every function here: the firm comes first and is required.

Exports: `getMatter`, `listMatters`, `countMatters`, `matterCountsByStatus`, `MatterFilters`

### `src/lib/data/onboarding.ts`

Each step is saved as it is answered, so "Save as draft" is not a separate feature: leaving halfway through and coming back is simply what the questionnaire does.

Exports: `loadDraft`, `saveStep`, `completeOnboarding`, `restartOnboarding`, `matterTypeOptions`, `OnboardingDraft`

### `src/lib/data/scope.ts`

Every data-access function in `src/lib/data` takes one of these as its first argument.

Exports: `isFirmScope`, `FirmScope`

### `src/lib/data/search.ts`

Search is the easiest place in any multi-tenant product to leak data: it touches several tables at once, and a single query written without the firm would surface anot…

Exports: `searchFirm`, `SearchResult`

### `src/lib/data/statistics.ts`

These are the numbers the dashboard puts in large type, which makes them the numbers a user trusts without checking.

Exports: `firmStatistics`, `FirmStatistics`

### `src/lib/data/usage.ts`

Costs are billed per firm, so a leak here would not merely expose data — it would produce a wrong invoice.

Exports: `usageSummary`, `listUsageRecords`, `formatCost`, `UsageSummary`

### `src/lib/demo-accounts.ts`

Every account is fictional and uses the reserved `.local` domain, which cannot exist on the real internet.

Exports: `visibleDemoAccounts`, `DEMO_PASSWORD`, `DEMO_ACCOUNTS`, `DemoAccount`

### `src/lib/env.ts`

Every environment variable the application depends on is read here, once, and validated.

Exports: `parseServerEnv`, `serverEnv`, `AI_PROVIDERS`, `APP_ENVIRONMENTS`, `EnvironmentError`, `AiProviderName`, `AppEnvironment`, `EnvSource`, `ServerEnv`

### `src/lib/http/form-post.ts`

Orchelio submits its consequential forms — switching firm, saving an onboarding step, confirming a configuration — to route handlers that answer with a 303, rather tha…

Exports: `isSameOrigin`, `seeOther`

### `src/lib/json-field.ts`

Prisma does not support the Json type on SQLite, so structured payloads — firm configuration, matter fields, AI results — are stored as JSON text.

Exports: `parseJsonObject`, `parseStringArray`, `toJsonColumn`

### `src/lib/onboarding/catalogue.ts`

Everything the questionnaire offers is declared here as data.

Exports: `resolveKey`, `LANGUAGES`, `CURRENCIES`, `TIMEZONES`, `JURISDICTIONS`, `WORKFLOW_STEP_OPTIONS`, `AI_FEATURE_OPTIONS`, `CONFIGURABLE_APPROVAL_OPTIONS`, `LOCKED_APPROVAL_OPTIONS`, `ScopedKey`, `WorkflowStepOption`, `AiFeatureOption`, `ApprovalOption`

### `src/lib/onboarding/config.ts`

Pure functions, no database and no request.

Exports: `workflowKeysFor`, `aiFeatureKeysFor`, `workflowIdsFrom`, `aiFeatureIdsFrom`, `buildApprovals`, `buildConfiguration`, `validateStep`, `sampleAnswersFor`, `ONBOARDING_STEP_COUNT`, `ONBOARDING_STEPS`, `OnboardingAnswers`, `FirmConfigurationPayload`, `StepValidation`

### `src/lib/practice-areas.ts`

The onboarding questionnaire (Phase 4) offers this list.

Exports: `practiceAreaLabel`, `isPracticeAreaAvailable`, `PRACTICE_AREAS`, `PracticeAreaKey`, `PracticeArea`

### `src/lib/prisma.ts`

Next.js hot-reloads modules in development, which would otherwise open a new SQLite connection on every save until the process runs out of handles.

Exports: `prisma`

### `src/lib/roadmap.ts`

The demonstration is built in nine phases.

Exports: `currentPhase`, `PHASES`, `PhaseStatus`, `Phase`

### `src/lib/system-status.ts`

The home page is not a static mock-up: it runs this check on every request so that a broken install is visible immediately, with the exact command needed to fix it.

Exports: `getDatabaseStatus`, `getSystemStatus`, `listFirms`, `DatabaseStatus`, `SystemStatus`

## `tests/`

### `tests/setup.ts`

## `tests/e2e/`

### `tests/e2e/auth.spec.ts`

Phase 2 acceptance.

### `tests/e2e/firm-isolation.spec.ts`

Phase 3 acceptance, in a real browser.

### `tests/e2e/home.spec.ts`

Phase 1 acceptance: the application actually runs, renders under the Orchelio identity, warns that this is a demonstration, and reports live database state rather than…

### `tests/e2e/onboarding.spec.ts`

Phase 4 acceptance, in a real browser.

## `tests/integration/`

### `tests/integration/data-access.test.ts`

The sibling suite (isolation.test.ts) proves the database client refuses an unscoped query.

### `tests/integration/fixtures.ts`

Isolation cannot be proved against the demonstration seed: it has no matters, and a test that passes because there is nothing to leak proves nothing.

Exports: `createTwoFirmFixture`, `SHARED_CLIENT_NAME`, `SHARED_DOCUMENT_NAME`, `SHARED_MATTER_TITLE`, `TwoFirmFixture`, `FirmFixture`

### `tests/integration/isolation.test.ts`

This is the suite the whole product rests on.

## `tests/stubs/`

### `tests/stubs/server-only.ts`

Stand-in for the `server-only` package during tests.

## `tests/unit/`

### `tests/unit/app-identity.test.ts`

Product naming is a requirement of the specification, not a cosmetic detail: the demonstration must never present itself under a generic name.

### `tests/unit/cache.test.ts`

A cache is a place where one request's answer is handed to another.

### `tests/unit/demo-accounts.test.ts`

### `tests/unit/demo-banner.test.tsx`

### `tests/unit/env.test.ts`

The environment layer is the single gate for configuration.

### `tests/unit/firm-context.test.ts`

The active-firm cookie is a preference, not a credential.

### `tests/unit/firm-scope.test.ts`

The guard is the backstop that turns a forgotten `where` clause from a silent data leak into a loud failure.

### `tests/unit/json-field.test.ts`

These columns hold firm configuration and AI results.

### `tests/unit/onboarding-config.test.ts`

The acceptance criterion for this phase: answering the questionnaire must reproduce the two configurations printed in the specification.

### `tests/unit/password.test.ts`

### `tests/unit/permissions.test.ts`

The permission matrix is a safety property, not a convenience.

### `tests/unit/rate-limit.test.ts`
